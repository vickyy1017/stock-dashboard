import express from "express";

// Minimal server router - this project uses static frontend only
// The server just serves the built static files
const router = express.Router();

export default router;
